apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gateway-api.fullname" . }}-secret
  labels:
    {{- include "gateway-api.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.secrets.clientSecret | b64enc }}