apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "convert.fullname" . }}-test-connection"
  labels:
    {{- include "convert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "convert.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
