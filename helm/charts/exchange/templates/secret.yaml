apiVersion: v1
kind: Secret
metadata:
  name: {{ include "exchange.fullname" . }}-secret
  labels:
    {{- include "exchange.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.secrets.clientSecret | b64enc }}
  db-password: {{ .Values.secrets.dbPassword | b64enc }}