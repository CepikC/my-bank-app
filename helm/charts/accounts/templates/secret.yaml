apiVersion: v1
kind: Secret
metadata:
  name: {{ include "accounts.fullname" . }}-secret
  labels:
    {{- include "accounts.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.secrets.clientSecret | b64enc }}
  db-password: {{ .Values.secrets.dbPassword | b64enc }}