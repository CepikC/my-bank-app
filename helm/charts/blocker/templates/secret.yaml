apiVersion: v1
kind: Secret
metadata:
  name: {{ include "blocker.fullname" . }}-secret
  labels:
    {{- include "blocker.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.secrets.clientSecret | b64enc }}