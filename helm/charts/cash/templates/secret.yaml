apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cash.fullname" . }}-secret
  labels:
    {{- include "cash.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.secrets.clientSecret | b64enc }}